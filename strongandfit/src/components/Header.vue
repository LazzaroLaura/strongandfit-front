<template>

<header>

    <div>
        <h6>Bonjour</h6>

        <button>Déconnexion</button>
    </div>

</header>

</template>

<script>
//import userService from '../services/userService.js';

export default {
/*      methods: {
        disconnect: function() {
            // descruction des données dans le session storage
            userService.deleteCurrentUserData();
            // nous "forçons" le header pour qu'il considère qu'il n'y a plus d'utilisateur connecté
            this.currentUser = false;
            // BONUS après déconnexion, redirige l'utilisateur vers la home
            this.$router.push({
                name: "home"
            });
        },
    }, */
}

</script>

<style scoped>
</style>